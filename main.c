#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Student 구조체 정의
typedef struct {
    int id;          // 학번 (정수)
    char name[20];   // 이름 (문자열)
    double score;    // 점수 (실수)
} Student;

int main(void)  {    

    printf("Hello World!");
    
    return 0;
}
